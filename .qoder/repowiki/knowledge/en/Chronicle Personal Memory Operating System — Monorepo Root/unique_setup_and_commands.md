npm run dev builds and serves the app via Vite on port 5173; npm run build produces the production bundle; npm run test runs Vitest against src; npm run test:visual invokes tests/visual/run.py for screenshot diffing; npm run lint/format enforce code quality; asset provisioning scripts (ddg_download_all.js, curated_download.js, restore*.cjs, etc.) are executed directly from the workspace root.